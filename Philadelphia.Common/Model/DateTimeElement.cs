namespace Philadelphia.Common {
    /// <summary>
    /// helper to handle different days in month
    /// </summary>
    public enum DateTimeElement {
        Year,
        Month,
        Day,
        Hour,
        Minute,
        Second
    }
}
