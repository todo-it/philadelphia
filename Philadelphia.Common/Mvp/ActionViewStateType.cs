namespace Philadelphia.Common {
    // Question: merge with ActionViewState class maybe (nest under 'Types' name)?
    public enum ActionViewStateType {
        IdleOrSuccess,
        OperationRunning,
        OperationFailed
    }
}
