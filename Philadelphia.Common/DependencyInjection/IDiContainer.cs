namespace Philadelphia.Common {
    // review: seems that this interface is obsolete and could be inlined
    public interface IDiContainer : IDiRegisterOnlyContainer, IDiResolveReleaseOnlyContainer {
    }
}
